export enum USER_ROLES {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum STATUS {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
  BLOCKED = 'BLOCKED',
  SUSPENDED = 'SUSPENDED',
}

export enum GENDER {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other'
}